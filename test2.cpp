#include<bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        string a, b;
        cin >> a >> b;
        int k;
        cin >> k;
        string aft;
        string tmp;
        int l = min(a.length(), b.length());
        int la = a.length();
        int lb = b.length();
        for (int i = 1;i <= l;i++) {
            if (a[])
        }
    }
    return 0;
}